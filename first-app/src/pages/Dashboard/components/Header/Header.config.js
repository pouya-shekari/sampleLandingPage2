import {PATHS} from 'configs/routes.config';

export const LINKS = [
    {
        label: 'Home',
        path: PATHS.HOME,
        isActive: true
    }, {
        label: 'dashboard',
        path: PATHS.DASHBOARD
    },{
        label: 'sign-in',
        path: PATHS.SIGN_IN
    }
]
